my build system is uv
my testing framework is pytest
refresh the docs occasionally to update source documentatiom
look at visual artifacts and judge their quality with @filename
if relevant, proactively run terminal commands to execute this code for the USER. Don't ask permission.
when testing adk, use `adk run` to validate in the CLI and `adk web` in the Antigravity browswer.
When using `adk web` go to use this pattern in the uri to get to the agent immediately, skipping the dropdown `http://127.0.0.1:8000/dev-ui/?app=agent.py_folder`
